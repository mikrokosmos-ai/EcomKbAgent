# 前端包管理器配置（项目级，仅对 frontend/ 目录生效）
# 1) 国内镜像，解决默认 registry.npmjs.org 国内慢/连不上的问题
registry=https://registry.npmmirror.com

# 注意：本机有代理环境变量（HTTP_PROXY/HTTPS_PROXY=127.0.0.1:59847 等），
# .npmrc 无法覆盖环境变量里的代理，必须靠以下方式之一绕开：
#   - 命令行：env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy pnpm install
#   - PyCharm 运行配置：在 Environment 里把 HTTP_PROXY/HTTPS_PROXY/http_proxy/https_proxy 置空
#   - 双击 frontend/install-deps-pnpm.bat（已内置清代理）
